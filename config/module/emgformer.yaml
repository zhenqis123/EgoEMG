defaults:
  - featurizer: tds
  - decoder: transformer/preset/small
  - head: mlp
  - _self_

_target_: egoemg.models.modules.emgformer.Emg2PoseFormer

# Common settings
out_channels: 22
decoder:
  causal: true
  activation: relu
  norm_first: true
  pos_encoding: rope
  out_proj: true
