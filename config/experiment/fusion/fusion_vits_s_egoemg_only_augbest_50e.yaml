# @package _global_
defaults:
  - /experiment/fusion/fusion_frozenvision_s_egoemg_only_augbest_50e
  - _self_

vision_vit_checkpoint: data/experiment_inputs/vision_checkpoints/vits.ckpt

module:
  vision_backbone_type: vit_small
  vision_embed_dim: 384
  vision_pretrained_checkpoint: ${vision_vit_checkpoint}

logger:
  name: fusion_vits_s_egoemg_only_augbest_50e

center_frame_eval: true
