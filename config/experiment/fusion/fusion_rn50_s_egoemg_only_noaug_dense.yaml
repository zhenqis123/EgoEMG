# @package _global_
# RN50-S controlled ablation: disable every EMG augmentation.
defaults:
  - /experiment/fusion/fusion_rn50_s_egoemg_only_augbest_no_mixup
  - override /augmentation: none
  - override /transforms: noop
  - _self_

logger:
  name: fusion_rn50_s_egoemg_only_noaug_dense

center_frame_eval: true
