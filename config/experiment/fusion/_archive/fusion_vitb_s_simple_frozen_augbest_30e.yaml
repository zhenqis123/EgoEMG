# @package _global_
defaults:
  - /experiment/fusion/fusion_simple_frozenvision_s_egoemg_only_augbest_30e
  - _self_

vision_vit_checkpoint: data/experiment_inputs/vision_checkpoints/vitb.ckpt

module:
  vision_backbone_type: vit_base
  vision_embed_dim: 768
  vision_pretrained_checkpoint: ${vision_vit_checkpoint}

logger:
  name: fusion_vitb_s_simple_frozen_augbest_30e

center_frame_eval: true
