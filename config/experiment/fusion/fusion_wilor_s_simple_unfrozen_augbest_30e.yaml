# @package _global_
defaults:
  - /experiment/fusion/fusion_simple_unfrozenvision_s_egoemg_only_augbest_30e
  - _self_

vision_wilor_checkpoint: data/experiment_inputs/vision_checkpoints/wilor.ckpt

module:
  vision_backbone_type: vit
  vision_embed_dim: 1280
  vision_pretrained_checkpoint: ${vision_wilor_checkpoint}

logger:
  name: fusion_wilor_s_simple_unfrozen_augbest_30e
